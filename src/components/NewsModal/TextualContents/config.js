const appPrefix = '/';
const basePath = window.location.origin + appPrefix;

const textualContentPath = basePath + 'textualcontents';

export default {
  // path configuration
  textualContentPath
};
